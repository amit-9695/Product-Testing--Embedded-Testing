# Splitting a Strings
# The split() method in Python is used to split a string into a list of substrings based on a specified delimiter. If no delimiter is provided, it defaults to splitting by whitespace.

name= "Amit Kumar"
print(name.split())

components = name.split()
print(components[0])