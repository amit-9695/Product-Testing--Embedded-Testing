#include<stdio.h>
#include<string.h>

void main(){
    char str[10];
    int i;
    printf("Enter a string: ");
    scanf("%s", str);

    for (i=0; str[i] != '\0'; i++) {
    }
    printf("length of string: %d\n", i);
}